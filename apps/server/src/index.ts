import express from "express" 
const app = express(); 

// Enable JSON parsing
app.use(express.json())

//google steets api 
// user-> frontend -> registration form -> user -> data -> handle to that data > node endpoint /// where a palce where we connect over data -> google sheets -> 
//
// summary 
//  backend is noting but a bridge between connection to data  and google sheets 
//
//  app.
//
//
//
//
//
//

	app.post(res,req()=>{
		const data = res.body(); 
			
	})
